version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/angular-amplify
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*